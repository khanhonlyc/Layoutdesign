// $('.main-slider').slick();
// $('.slider').slick();
$('.slider').slick({
    // slidesToShow: 3,
    slidesToScroll: 1,
    autoplay: true,
    dots: true,
    draggable : false,
    autoplaySpeed: 5000,
  });
      